<tr id="<%= dom_id user %>">
  <td><%= user.email %></td>
  <td><%= user.roles.first&.name&.humanize %></td>
  <td>
<!--    <a href="#"><i class="material-icons">edit</i><span class="hide-menu"></span></a>-->
    <%= link_to user_path(user), data: { turbo: true, turbo_method: :delete, turbo_confirm: "Are you sure?" } do %>
      <i class="material-icons">delete</i><span class="hide-menu"></span>
    <% end %>
  </td>
</tr>